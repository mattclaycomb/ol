{{pagination-links pages=businesses.meta}}
Per Page:
<select type="text" value={{per_page}} onChange={{action (mut per_page) value="target.value"}}>
  <option value="25">25</option>
  <option value="50">50</option>
  <option value="100">100</option>
</select>

<table class="table business-table">
  {{#each businesses as |business|}}
    <tr class="business-row">
      <td>{{business.id}}</td>
      <td>{{link-to business.name 'businesses.detail' business}}</td>
    </tr>
  {{/each}}
</table>

{{pagination-links pages=businesses.meta}}
